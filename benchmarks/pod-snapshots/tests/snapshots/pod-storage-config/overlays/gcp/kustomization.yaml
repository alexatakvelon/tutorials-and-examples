apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - path: patch.yaml
    target:
      group: podsnapshot.gke.io
      version: v1alpha1
      kind: PodSnapshotStorageConfig
      name: ps-storage
